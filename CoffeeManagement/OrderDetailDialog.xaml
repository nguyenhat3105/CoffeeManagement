<Window x:Class="CoffeeManagement.OrderDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chi tiết đơn hàng" Height="450" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtOrderInfo"
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="OrderItemsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#ccc" BorderThickness="1"
                                CornerRadius="5" Padding="8" Margin="4">
                            <StackPanel>
                                <TextBlock Text="{Binding MenuItem.Name}"
                                           FontWeight="Bold" FontSize="14"/>
                                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                    <TextBlock Text="{Binding Quantity, StringFormat='Số lượng: {0}'}" Width="150"/>
                                    <TextBlock Text="{Binding UnitPrice, StringFormat='Đơn giá: {0:C0}'}" Width="150"/>
                                    <TextBlock Text="{Binding TotalPrice, StringFormat='Thành tiền: {0:C0}'}"
                                               FontWeight="Bold"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock Text="Tổng cộng:" FontWeight="Bold" Margin="0,0,6,0"/>
            <TextBlock x:Name="TxtTotalAmount"
                       FontWeight="Bold" Foreground="Green"/>
        </StackPanel>
    </Grid>
</Window>
