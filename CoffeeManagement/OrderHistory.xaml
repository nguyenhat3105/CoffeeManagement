<UserControl x:Class="CoffeeManagement.OrderHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:CoffeeManagement"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <SolidColorBrush x:Key="AccentColor" Color="#D4AF37"/>
        <SolidColorBrush x:Key="AccentForeground" Color="#2A2A2A"/>
        <SolidColorBrush x:Key="AccentHover" Color="#E6B93F"/>
        <SolidColorBrush x:Key="PrimaryTextColor" Color="#333"/>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="#666"/>
        <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="StatusPendingBackground" Color="#FFF8E1"/>
        <SolidColorBrush x:Key="StatusPendingForeground" Color="#FFA000"/>
        <SolidColorBrush x:Key="StatusCompletedBackground" Color="#E8F5E9"/>
        <SolidColorBrush x:Key="StatusCompletedForeground" Color="#388E3C"/>
        <SolidColorBrush x:Key="StatusCancelledBackground" Color="#FFEBEE"/>
        <SolidColorBrush x:Key="StatusCancelledForeground" Color="#D32F2F"/>
        <SolidColorBrush x:Key="StatusDefaultBackground" Color="#ECEFF1"/>
        <SolidColorBrush x:Key="StatusDefaultForeground" Color="#546E7A"/>

        <Style x:Key="ViewDetailsButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="{StaticResource AccentForeground}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusBadgeStyle" TargetType="Label">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Background" Value="{StaticResource StatusDefaultBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource StatusDefaultForeground}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Label">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Content, RelativeSource={RelativeSource Self}}" Value="Pending">
                    <Setter Property="Background" Value="{StaticResource StatusPendingBackground}"/>
                    <Setter Property="Foreground" Value="{StaticResource StatusPendingForeground}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Content, RelativeSource={RelativeSource Self}}" Value="Completed">
                    <Setter Property="Background" Value="{StaticResource StatusCompletedBackground}"/>
                    <Setter Property="Foreground" Value="{StaticResource StatusCompletedForeground}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Content, RelativeSource={RelativeSource Self}}" Value="Cancelled">
                    <Setter Property="Background" Value="{StaticResource StatusCancelledBackground}"/>
                    <Setter Property="Foreground" Value="{StaticResource StatusCancelledForeground}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Color="#E0E0E0" Opacity="0.7" BlurRadius="10"/>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🧾 Lịch sử mua hàng"
                   FontSize="24"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource PrimaryTextColor}"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="OrdersList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                                    CornerRadius="8" Padding="20"
                                    Margin="0,0,8,12" Background="{StaticResource CardBackground}">
                                <Border.Effect>
                                    <DropShadowEffect ShadowDepth="1" Color="#E0E0E0" Opacity="0.7" BlurRadius="10"/>
                                </Border.Effect>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Id, StringFormat='Order #{0}'}"
                                                   FontWeight="SemiBold" FontSize="16"
                                                   Foreground="{StaticResource PrimaryTextColor}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='Ngày tạo: {0:dd/MM/yyyy HH:mm}'}"
                                                   Grid.Column="1"
                                                   FontSize="12" Foreground="{StaticResource SecondaryTextColor}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                    <Grid Grid.Row="1" Margin="0,12,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                    
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding TotalAmount, StringFormat='{}{0:N0}đ'}"
                                                   FontWeight="Bold" FontSize="18"
                                                   Foreground="{StaticResource AccentColor}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                    <Button Grid.Row="2"
                                            Content="Xem chi tiết"
                                            Click="BtnViewDetails_Click"
                                            HorizontalAlignment="Right"
                                            Margin="0,15,0,0"
                                            Style="{StaticResource ViewDetailsButtonStyle}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <StackPanel Grid.Column="1" Margin="20,0,0,0">

                <TextBlock Text="📊 Thống kê của bạn"
                           FontSize="20" FontWeight="SemiBold"
                           Foreground="{StaticResource PrimaryTextColor}"
                           Margin="0,0,0,15"/>

                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="TỔNG CHI TIÊU"
                                   FontSize="12" FontWeight="Bold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtTotalSpent"
                                   Text="0đ"
                                   FontSize="24" FontWeight="Bold"
                                   Foreground="{StaticResource AccentColor}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="TỔNG SỐ ĐƠN HÀNG"
                                   FontSize="12" FontWeight="Bold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtTotalOrders"
                                   Text="0"
                                   FontSize="24" FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryTextColor}"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <TextBlock Text="MÓN YÊU THÍCH"
                                   FontSize="12" FontWeight="Bold"
                                   Foreground="{StaticResource SecondaryTextColor}"
                                   Margin="0,0,0,5"/>
                        <TextBlock x:Name="TxtFavoriteItem"
                                   Text="Chưa có"
                                   FontSize="24" FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryTextColor}"/>
                    </StackPanel>
                </Border>

            </StackPanel>

        </Grid>
    </Grid>
</UserControl>